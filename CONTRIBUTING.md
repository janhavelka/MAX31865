# Contributing

Keep changes focused and covered by native tests where practical.

Before opening a pull request:

- Run `pio test -e native`.
- Run `pio run -e esp32s3dev` when Arduino-facing code changes.
- Keep public API changes reflected in `README.md` and `CHANGELOG.md`.
